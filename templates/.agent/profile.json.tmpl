{
  "frameworkVersion": "{{FRAMEWORK_VERSION}}",
  "project": {
    "name": "{{PROJECT_NAME}}",
    "stack": {
      "nextVersion": "{{NEXT_VERSION}}",
      "stateLib": "{{STATE_LIB}}",
      "i18nLib": "{{I18N_LIB}}",
      "i18nLocalesPath": "{{I18N_LOCALES_PATH}}",
      "uiPackage": "{{UI_PACKAGE}}"
    },
    "apps": {{APPS_JSON}}
  },
  "paths": {
    "frameworkRoot": "{{FRAMEWORK_ROOT}}",
    "profileRoot": ".agent",
    "extractRoot": "_extract",
    "sharedRoot": "{{SHARED_ROOT}}"
  },
  "scripts": {
    "verify": "{{VERIFY_SCRIPT}}"
  },
  "mcp": {
    "figmaEnv": ".env.figma",
    "confluenceEnv": ".env.confluence"
  }
}
